%h1#name= @cat['name']
%h3
  %a{href: @cat["_id"].to_s + "/items"} Items

%h3 Fields
%form(method="POST")
  .entry
    %input(name="name")
  %input(type="submit")
  
%ul
  - @cat['fields'].each do |field|
    %li= field["name"]